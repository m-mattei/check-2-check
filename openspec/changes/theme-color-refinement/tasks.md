## 1. Global Theme Refinement

- [ ] 1.1 In `lib/main.dart`, define a persistent secondary color in the `ColorScheme`.
- [ ] 1.2 Verify that the `secondary` color contrast is accessible in both Light and Dark modes.

## 2. Calendar Styling Implementation

- [ ] 2.1 Navigate to `lib/screens/calendar_screen.dart`.
- [ ] 2.2 Update `todayDecoration` to use `colorScheme.secondaryContainer` or an equivalent secondary-themed color.
- [ ] 2.3 Update `selectedDecoration` to use `colorScheme.secondary`.
- [ ] 2.4 Set the `selectedTextStyle` to use `colorScheme.onSecondary` to maintain contrast.

## 3. Verification

- [ ] 3.1 Verify calendar selection styling in Light Mode.
- [ ] 3.2 Verify calendar selection styling in Dark Mode.
