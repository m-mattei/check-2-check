## 1. Data Models

- [x] 1.1 Create `lib/models/household_role.dart` for the `HouseholdRole` enum.
- [x] 1.2 Create `lib/models/user.dart` for the internal `User` data class.
- [x] 1.3 Create `lib/models/person.dart` for the `Person` data class (linked to User or virtual).
- [x] 1.4 Create `lib/models/household.dart` for the `Household` and `HouseholdMember` data classes.

## 2. Verification

- [x] 2.1 Verify that the application builds and the new files compile without errors.
