## 1. Dependencies

- [x] 1.1 Add `table_calendar: ^3.1.2` to the `dependencies` section of `pubspec.yaml`.
- [x] 1.2 Run `flutter pub get` to install the new package.

## 2. UI Implementation

- [x] 2.1 Create `lib/screens/calendar_screen.dart` with a basic `Scaffold` and a `TableCalendar` widget.
- [x] 2.2 Implement state management for `_selectedDay` and `_focusedDay` in the `CalendarScreen` state.

## 3. Navigation

- [x] 3.1 Modify `lib/main.dart` to set `CalendarScreen` as the `home` of `MaterialApp`.

## 4. Verification

- [x] 4.1 Run the web app and verify the calendar is interactive and highlights the selected day.
